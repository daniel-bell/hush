{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IpHistory</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Ip</th>
                <td>{{ entity.ip }}</td>
            </tr>
            <tr>
                <th>Accesstime</th>
                <td>{{ entity.accessTime|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ip_history') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ip_history_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
