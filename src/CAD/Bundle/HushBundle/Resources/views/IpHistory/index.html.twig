{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IpHistory list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Ip</th>
                <th>Accesstime</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ip_history_show', { 'id': entity.id }) }}">{{ entity.ip }}</a></td>
                <td>{% if entity.accessTime %}{{ entity.accessTime|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ip_history_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ip_history_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ip_history_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
